buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Устанавливаем стабильную версию плагина 8.9.1
        classpath 'com.android.tools.build:gradle:8.9.1' 
        classpath 'com.google.gms:google-services:4.4.2'
    }
}

apply plugin: 'com.android.application'

android {
    namespace "com.trivo.app"
    compileSdk 36

    defaultConfig {
        applicationId "com.trivo.app"
        minSdkVersion 22
        targetSdkVersion 36
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    // Настройки линтера для пропуска проверок SDK
    lintOptions {
        checkReleaseBuilds false
        abortOnError false
    }

    // Единственно верная настройка Java 21 для Android-проекта
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_21
        targetCompatibility JavaVersion.VERSION_21
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

repositories {
    google()
    mavenCentral()
}

// Принудительная фиксация версий core-библиотек
configurations.all {
    resolutionStrategy.force "androidx.core:core:1.15.0"
    resolutionStrategy.force "androidx.core:core-ktx:1.15.0"
}

dependencies {
    // Поддержка локальных файлов без предупреждений flatDir
    implementation fileTree(dir: 'libs', include: ['*.jar', '*.aar'])
    
    // Фиксируем core на 1.15.0 для стабильности сборки
    implementation "androidx.core:core:1.15.0"
    implementation "androidx.core:core-ktx:1.15.0"
    
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation "androidx.coordinatorlayout:coordinatorlayout:$androidxCoordinatorLayoutVersion"
    implementation "androidx.core:core-splashscreen:$coreSplashScreenVersion"
    implementation project(':capacitor-android')
    
    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
    implementation project(':capacitor-cordova-android-plugins')

    // Firebase (конфигурация сканера)
    implementation platform('com.google.firebase:firebase-bom:33.5.1')
    implementation 'com.google.firebase:firebase-messaging'
    implementation 'com.google.firebase:firebase-analytics'
}

apply from: 'capacitor.build.gradle'

// Безопасное подключение Google Services
try {
    if (file('google-services.json').exists()) {
        apply plugin: 'com.google.gms.google-services'
    }
} catch(Exception e) {
    logger.info("google-services.json not found")
}
